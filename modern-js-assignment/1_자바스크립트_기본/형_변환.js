/**
 * 형변환 : type conversion
 */

/**
 * 문자형으로 변환
 *
 * alert에서 value는 문자형이어야 한다.
 */

/**
 * 숫자형으로 변환
 *
 * 수학과 관련된 함수와 표현식에서 자동 변경
 * 예) '6' / "2"
 *
 * 다른 -> 숫자형 규칙
 * undefined -> NaN
 * null -> 0
 * treu and false -> 1 / 0
 * string -> 공벡 제거 후 남아있느 문자열 없으면 0 , 있으면 문자열에서 숫자읽기
 *          변환 실패 시 NaN
 */

/**Boolean으로 변환
 * 숫자 0, 빈문자열 , null, undefined, Nan -> false
 * others : ture
 * 주의 : 문자열 '0'은 트루
 */
